<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bartek.komunappmobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SignupActivity"></activity>
        <activity android:name=".Activities.flat.AddFlat" />
        <activity android:name=".Activities.MainActivity"></activity>
        <activity android:name=".Activities.shopping.ShoppingActivity" />
        <activity android:name=".Activities.shopping.AddList" />
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.flat.FlatActivity" />
        <activity android:name=".Activities.discussion.DiscussionActivity" />
        <activity android:name=".Activities.payment.PaymentActivity" />
        <activity android:name=".Activities.task.TaskActivity" />
        <activity
            android:name=".Activities.shopping.OpenList"
            android:label="@string/title_activity_open_list"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>